# 비용 최적화 배포 아키텍처 설계

## 목표
개인 블로그/포트폴리오 사이트를 **월 비용 $0~$5 이내**로 운영 가능한 아키텍처 설계

---

## 현재 스택 분석

### 프론트엔드
- **Next.js 15** (App Router, React Server Components)
- **Static + Dynamic** 페이지 혼재
- 이미지 업로드 및 미디어 관리

### 백엔드
- **NestJS** (Node.js)
- **PostgreSQL** 데이터베이스
- **TypeORM**
- REST API
- JWT 인증

### 주요 기능
- 블로그 포스팅 (CRUD)
- 프로젝트 관리
- 댓글 시스템
- 프로필/경력 관리
- 이미지 업로드
- 문의 메시지

---

## 아키텍처 옵션 비교

### 옵션 1: 완전 무료 티어 (월 $0)

#### 구성
```
┌─────────────────────────────────────────────────┐
│  Frontend: Vercel (Free Tier)                   │
│  - Next.js Static + SSG + ISR                   │
│  - Edge Functions (제한적 사용)                  │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│  Backend: Railway / Render (Free Tier)          │
│  - NestJS API (Sleep after 15min inactivity)    │
│  - 512MB RAM, 0.5 CPU                           │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│  Database: Neon / Supabase (Free Tier)          │
│  - PostgreSQL 512MB                             │
│  - 3GB storage                                  │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│  File Storage: Cloudflare R2 (Free Tier)        │
│  - 10GB storage                                 │
│  - S3-compatible API                            │
└─────────────────────────────────────────────────┘
```

#### 서비스별 무료 티어 한도

**Vercel (Frontend)**
- 100GB 대역폭/월
- 6,000 빌드 시간(분)/월
- 100 Serverless Function 실행/일
- 자동 SSL, CDN 포함
- **제약**: Commercial use 가능, 하지만 과도한 트래픽 시 유료 전환 필요

**Railway.app (Backend) - 무료 티어**
- $5 크레딧/월 (약 500시간)
- 512MB RAM, 1GB 디스크
- Sleep after 15분 미사용
- **제약**: 첫 요청 시 Cold Start (5-10초)

**Render.com (Backend 대안)**
- 750시간/월 무료
- 512MB RAM
- Sleep after 15분 미사용
- **제약**: Cold Start 존재

**Neon.tech (Database)**
- PostgreSQL Serverless
- 512MB storage
- 3GB 데이터 전송/월
- Auto-pause (5분 미사용 시)
- **제약**: Connection 한도 (20 concurrent)

**Supabase (Database 대안)**
- PostgreSQL 500MB
- 2GB 파일 스토리지
- 50MB 데이터베이스 사이즈
- Edge Functions 500,000 호출/월
- **제약**: Realtime 리스너 제한

**Cloudflare R2 (이미지 스토리지)**
- 10GB storage
- 1,000,000 Class A 요청/월 (PUT, POST)
- 10,000,000 Class B 요청/월 (GET, HEAD)
- Egress 무료 (대역폭 무제한!)
- **장점**: S3 호환, Egress 비용 없음

**Cloudinary (이미지 호스팅 대안)**
- 25GB storage
- 25GB 대역폭/월
- 이미지 변환 포함
- **제약**: 무료 티어는 transformation 제한적

#### 장점
- ✅ **완전 무료**
- ✅ SSL, CDN 자동 제공
- ✅ CI/CD 자동화
- ✅ 개인 블로그 트래픽으로 충분

#### 단점
- ❌ Backend Cold Start (첫 요청 5-10초 지연)
- ❌ 동시 접속자 많으면 응답 느림
- ❌ 무료 티어 제약 (DB 크기, RAM 등)
- ❌ 상업적 목적 제약 (Vercel)

#### 최적화 전략
1. **ISR (Incremental Static Regeneration) 적극 활용**
   - 블로그 포스트: `revalidate: 3600` (1시간)
   - 프로젝트 목록: `revalidate: 7200` (2시간)
   - 프로필: `revalidate: 86400` (24시간)

2. **Cold Start 완화**
   - Vercel Serverless Functions에서 캐싱된 데이터 직접 서빙
   - Backend는 쓰기 작업(관리자)과 캐시 갱신만 담당
   - UptimeRobot 같은 무료 모니터링으로 15분마다 핑

3. **이미지 최적화**
   - Next.js Image Optimization (Vercel 자동 지원)
   - Cloudflare R2에 원본 저장 → Cloudflare CDN으로 서빙
   - WebP 포맷 자동 변환

4. **데이터베이스 연결 풀링**
   - Neon의 경우 Pooling 모드 사용
   - Connection limit 관리 (최대 5 concurrent)

---

### 옵션 2: 하이브리드 (월 $3~5)

#### 구성
```
┌─────────────────────────────────────────────────┐
│  Frontend: Vercel (Free Tier)                   │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│  Backend: Fly.io (Micro VM)                     │
│  - Shared CPU 256MB RAM                         │
│  - $1.94/월                                     │
│  - Always On (No Cold Start)                    │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│  Database: PlanetScale (Hobby)                  │
│  - MySQL Serverless                             │
│  - 5GB storage                                  │
│  - $0 (Hobby tier, 1 DB)                        │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│  File Storage: Cloudflare R2                    │
│  - 무료 티어                                    │
└─────────────────────────────────────────────────┘
```

#### 비용 상세
- **Fly.io**: $1.94/월 (256MB RAM, Shared CPU)
- **PlanetScale**: $0 (Hobby tier, 단 1개 DB만)
- **Cloudflare R2**: $0 (10GB 이내)
- **Vercel**: $0
- **총합**: ~$2/월

#### 장점
- ✅ **Cold Start 없음** (항상 켜져있음)
- ✅ 빠른 응답 속도
- ✅ 안정적인 성능
- ✅ 여전히 매우 저렴

#### 단점
- ❌ 약간의 비용 발생 ($2/월)
- ❌ PlanetScale Hobby tier는 1개 DB만 (dev/prod 분리 불가)

---

### 옵션 3: Static-First (월 $0)

#### 개념
**백엔드를 거의 사용하지 않고 Static + Edge로 해결**

#### 구성
```
┌─────────────────────────────────────────────────┐
│  Vercel (All-in-One)                            │
│  - Next.js Static Site Generation               │
│  - Edge Functions (관리자 API)                  │
│  - Vercel KV (Redis) for Auth & Cache           │
│  - Vercel Postgres (Beta, Free Tier)            │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│  Cloudflare R2 (이미지)                         │
└─────────────────────────────────────────────────┘
```

#### 아키텍처 변경 사항
1. **NestJS 제거**
   - Next.js API Routes로 대체
   - Edge Runtime 활용 (초고속 응답)

2. **데이터 관리**
   - 빌드 타임에 Static 페이지 생성
   - 관리자 페이지에서 글 작성 → Vercel Postgres에 저장
   - Webhook으로 Vercel 재배포 트리거
   - ISR로 변경 사항 점진적 반영

3. **인증**
   - Vercel KV (Redis)에 세션 저장
   - Edge Middleware로 관리자 인증

#### Vercel 무료 티어 한도 (Hobby)
- Vercel KV: 256MB, 100,000 commands/day
- Vercel Postgres: 256MB storage, 1 database
- Edge Functions: Unlimited (Global Edge Network)

#### 장점
- ✅ 완전 무료
- ✅ 초고속 (Edge CDN + SSG)
- ✅ 단순한 아키텍처
- ✅ Cold Start 없음
- ✅ 글로벌 배포

#### 단점
- ❌ 백엔드 리팩토링 필요
- ❌ Vercel에 강하게 종속됨
- ❌ 복잡한 쿼리/로직 구현 어려움
- ❌ 댓글 같은 실시간 기능 제약

---

### 옵션 4: Serverless 풀 스택 (월 $0~3)

#### 구성
```
┌─────────────────────────────────────────────────┐
│  Netlify (Frontend + Functions)                 │
│  - Next.js Static                               │
│  - Netlify Functions (Backend API)              │
│  - 125k 함수 호출/월                            │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│  Supabase (Free Tier)                           │
│  - PostgreSQL 500MB                             │
│  - Auth 포함                                    │
│  - Storage 1GB                                  │
│  - Realtime 포함                                │
└─────────────────────────────────────────────────┘
```

#### 장점
- ✅ 완전 무료 또는 매우 저렴
- ✅ Supabase Auth + Storage 통합
- ✅ Realtime 기능 (댓글 실시간 업데이트)

#### 단점
- ❌ 전체 아키텍처 마이그레이션 필요
- ❌ NestJS → Netlify Functions 변환

---

## 권장 아키텍처: 옵션 1 개선안

### 최종 구성
```
Frontend: Vercel (Free)
  ├─ Next.js 15 (SSG + ISR + Server Components)
  ├─ 관리자 페이지만 Dynamic
  ├─ 나머지 모든 페이지 Static
  └─ Vercel Analytics (무료)

Backend: Railway.app (Free $5 credit)
  ├─ NestJS API (Sleep 모드)
  ├─ 관리자 작업만 처리 (글쓰기, 수정, 삭제)
  └─ UptimeRobot으로 15분마다 Keep-Alive

Database: Neon.tech (Free Tier)
  ├─ PostgreSQL Serverless
  ├─ Auto-pause 활성화
  └─ Connection Pooling

File Storage: Cloudflare R2 (Free)
  ├─ 이미지 원본 저장
  └─ Cloudflare CDN으로 서빙

Monitoring: UptimeRobot (Free)
  └─ Backend Keep-Alive + Uptime 모니터링
```

### 구현 상세

#### 1. Frontend 최적화 (Vercel)

**next.config.js**
```javascript
module.exports = {
  output: 'standalone', // 빌드 최적화

  // ISR 설정
  experimental: {
    isrMemoryCacheSize: 0, // Edge에서 ISR 캐시 비활성화 (Vercel이 처리)
  },

  // 이미지 최적화
  images: {
    domains: ['pub-xxxxx.r2.dev'], // Cloudflare R2 도메인
    formats: ['image/avif', 'image/webp'],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920],
  },

  // 환경변수
  env: {
    NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL,
  },
}
```

**페이지별 revalidate 전략**
```typescript
// app/page.tsx (홈/프로필)
export const revalidate = 86400 // 24시간

// app/blog/page.tsx (블로그 목록)
export const revalidate = 3600 // 1시간

// app/blog/[slug]/page.tsx (블로그 포스트)
export const revalidate = 7200 // 2시간

// app/projects/page.tsx (프로젝트 목록)
export const revalidate = 7200 // 2시간

// app/admin/* (관리자 페이지)
export const dynamic = 'force-dynamic' // 항상 Dynamic
```

**API 캐싱 헬퍼**
```typescript
// lib/api-cache.ts
export async function fetchWithCache<T>(
  url: string,
  options: RequestInit = {}
): Promise<T> {
  return fetch(url, {
    ...options,
    next: {
      revalidate: 3600, // 1시간 캐시
      tags: ['api'], // Cache tag for on-demand revalidation
    },
  }).then(res => res.json())
}
```

**On-Demand Revalidation**
```typescript
// app/api/revalidate/route.ts
import { revalidateTag } from 'next/cache'
import { NextRequest, NextResponse } from 'next/server'

export async function POST(request: NextRequest) {
  const secret = request.nextUrl.searchParams.get('secret')

  if (secret !== process.env.REVALIDATION_SECRET) {
    return NextResponse.json({ message: 'Invalid token' }, { status: 401 })
  }

  const tag = request.nextUrl.searchParams.get('tag')

  if (tag) {
    revalidateTag(tag)
  }

  return NextResponse.json({ revalidated: true, now: Date.now() })
}
```

**Backend에서 캐시 무효화 호출**
```typescript
// backend/src/posts/posts.service.ts
async createPost(createPostDto) {
  const post = await this.postsRepository.save(createPostDto)

  // Vercel 캐시 무효화
  await fetch(
    `${process.env.FRONTEND_URL}/api/revalidate?secret=${process.env.REVALIDATION_SECRET}&tag=blog`,
    { method: 'POST' }
  )

  return post
}
```

#### 2. Backend 최적화 (Railway)

**railway.json**
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run start:prod",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}
```

**환경 변수**
```env
NODE_ENV=production
DATABASE_URL=postgresql://...  # Neon connection string
FRONTEND_URL=https://yourblog.vercel.app
REVALIDATION_SECRET=your-secret-key
JWT_SECRET=your-jwt-secret
CLOUDFLARE_R2_ENDPOINT=https://...
CLOUDFLARE_R2_ACCESS_KEY=...
CLOUDFLARE_R2_SECRET_KEY=...
```

**Health Check 엔드포인트**
```typescript
// backend/src/app.controller.ts
@Controller()
export class AppController {
  @Get()
  healthCheck() {
    return { status: 'ok', timestamp: new Date().toISOString() }
  }
}
```

#### 3. Database 최적화 (Neon)

**Connection Pooling 설정**
```typescript
// backend/src/app.module.ts
@Module({
  imports: [
    TypeOrmModule.forRootAsync({
      useFactory: () => ({
        type: 'postgres',
        url: process.env.DATABASE_URL,
        ssl: { rejectUnauthorized: false },
        // Neon Pooling 모드
        extra: {
          max: 5, // 최대 연결 수 (무료 티어 제한)
          connectionTimeoutMillis: 5000,
          idleTimeoutMillis: 30000,
        },
        autoLoadEntities: true,
        synchronize: false, // Production에서는 반드시 false
        logging: false,
      }),
    }),
  ],
})
```

**마이그레이션 전략**
```bash
# 로컬 개발
npm run migration:generate -- src/migrations/InitialSchema

# Production 배포 전 실행
npm run migration:run
```

#### 4. File Storage (Cloudflare R2)

**설정**
```typescript
// backend/src/media/media.service.ts
import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3'

@Injectable()
export class MediaService {
  private s3Client: S3Client

  constructor() {
    this.s3Client = new S3Client({
      region: 'auto',
      endpoint: process.env.CLOUDFLARE_R2_ENDPOINT,
      credentials: {
        accessKeyId: process.env.CLOUDFLARE_R2_ACCESS_KEY,
        secretAccessKey: process.env.CLOUDFLARE_R2_SECRET_KEY,
      },
    })
  }

  async uploadImage(file: Express.Multer.File): Promise<string> {
    const fileName = `${Date.now()}-${file.originalname}`

    await this.s3Client.send(
      new PutObjectCommand({
        Bucket: 'my-blog-images',
        Key: fileName,
        Body: file.buffer,
        ContentType: file.mimetype,
      })
    )

    return `https://pub-xxxxx.r2.dev/${fileName}`
  }
}
```

**Cloudflare R2 설정**
1. Cloudflare Dashboard → R2 → Create Bucket
2. Public Access 활성화 (또는 Custom Domain 연결)
3. API Token 생성 (R2 Read & Write)

#### 5. Keep-Alive 설정 (UptimeRobot)

**UptimeRobot 무료 티어**
- 50개 모니터
- 5분 간격 체크 (무료)
- SMS, Email 알림

**설정**
1. UptimeRobot 가입 (무료)
2. Monitor 추가
   - Type: HTTP(s)
   - URL: `https://your-backend.railway.app/`
   - Monitoring Interval: 5 minutes
3. Alert Contact 추가

**참고**: Railway 무료 티어는 15분 미사용 시 Sleep이므로, 5분 간격 핑으로 항상 깨워둠

---

## 배포 자동화 (CI/CD)

### GitHub Actions 워크플로우

**.github/workflows/deploy.yml**
```yaml
name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v20
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: ./frontend

  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Deploy to Railway
        run: |
          npm install -g railway
          railway login --token ${{ secrets.RAILWAY_TOKEN }}
          railway up
```

### Vercel 자동 배포
- GitHub 연동 시 자동으로 PR preview 및 main 브랜치 배포
- 환경변수 설정: Vercel Dashboard → Settings → Environment Variables

### Railway 자동 배포
- GitHub 연동 시 main 브랜치 push마다 자동 배포
- 환경변수 설정: Railway Dashboard → Variables

---

## 비용 시뮬레이션

### 시나리오 1: 개인 블로그 (월 1,000 방문자)
- **Vercel**: $0 (무료 티어 충분)
- **Railway**: $0 ($5 크레딧으로 충분)
- **Neon**: $0 (512MB 충분)
- **Cloudflare R2**: $0 (10GB 충분)
- **총합**: **$0/월**

### 시나리오 2: 중급 블로그 (월 10,000 방문자)
- **Vercel**: $0 (100GB 대역폭 내)
- **Railway**: $0 (Sleep 모드로 충분)
- **Neon**: $0 (512MB 충분)
- **Cloudflare R2**: $0 (10GB 충분)
- **총합**: **$0/월**

### 시나리오 3: 인기 블로그 (월 50,000 방문자)
- **Vercel**: $0 (Static 페이지는 캐싱되어 대역폭 적음)
- **Railway**: $0 (또는 Fly.io $2로 전환)
- **Neon**: $0 (512MB 충분, 읽기 위주)
- **Cloudflare R2**: $0 (Egress 무료)
- **총합**: **$0~2/월**

### 무료 티어 한계점
- **Vercel**: 100GB 대역폭 초과 시 ($40/100GB)
- **Railway**: $5 크레딧 소진 시 ($10/월부터 시작)
- **Neon**: 512MB 초과 시 ($19/월)

**대응 전략**:
1. Static 페이지 비율 극대화 (ISR)
2. 이미지 최적화 (WebP, Lazy Loading)
3. Cloudflare CDN 적극 활용 (Vercel은 Cloudflare 기반)
4. 한계 도달 시 Fly.io ($2/월)로 전환

---

## 마이그레이션 체크리스트

### Phase 1: 준비 (1일)
- [ ] Vercel 계정 생성 및 프로젝트 연결
- [ ] Railway 계정 생성
- [ ] Neon 계정 생성 및 데이터베이스 생성
- [ ] Cloudflare R2 버킷 생성
- [ ] 환경변수 설정 문서화

### Phase 2: Backend 배포 (1일)
- [ ] Railway에 NestJS 배포
- [ ] Neon DB 연결 확인
- [ ] Migration 실행
- [ ] Health Check 엔드포인트 확인
- [ ] Cloudflare R2 연동 테스트

### Phase 3: Frontend 최적화 (2일)
- [ ] next.config.js ISR 설정
- [ ] 페이지별 revalidate 설정
- [ ] API 캐싱 헬퍼 구현
- [ ] On-Demand Revalidation API 구현
- [ ] 이미지 최적화 설정

### Phase 4: 배포 및 테스트 (1일)
- [ ] Vercel에 Frontend 배포
- [ ] 환경변수 설정
- [ ] E2E 테스트 (관리자 기능)
- [ ] 성능 테스트 (Lighthouse)
- [ ] UptimeRobot 설정

### Phase 5: 모니터링 (진행 중)
- [ ] Vercel Analytics 확인
- [ ] Railway 메트릭 모니터링
- [ ] Neon 사용량 체크
- [ ] R2 스토리지 사용량 체크

---

## 성능 최적화 팁

### 1. Lighthouse 점수 목표
- Performance: 95+
- Accessibility: 100
- Best Practices: 95+
- SEO: 100

### 2. Core Web Vitals
- LCP (Largest Contentful Paint): < 2.5s
- FID (First Input Delay): < 100ms
- CLS (Cumulative Layout Shift): < 0.1

### 3. 최적화 체크리스트
- [ ] Image Optimization (Next.js Image)
- [ ] Font Optimization (next/font)
- [ ] Code Splitting (Dynamic Import)
- [ ] Tree Shaking
- [ ] Minification
- [ ] Compression (Brotli, Gzip)
- [ ] Lazy Loading
- [ ] Preloading Critical Resources
- [ ] Service Worker / PWA (선택)

---

## 백업 전략

### 데이터베이스 백업 (Neon)
```bash
# 주간 백업 스크립트
pg_dump $DATABASE_URL > backup-$(date +%Y%m%d).sql

# Cloudflare R2에 업로드
aws s3 cp backup-$(date +%Y%m%d).sql s3://my-blog-backups/ \
  --endpoint-url $CLOUDFLARE_R2_ENDPOINT
```

**GitHub Actions 자동 백업**
```yaml
name: Weekly Backup

on:
  schedule:
    - cron: '0 0 * * 0' # 매주 일요일

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Backup Database
        run: |
          pg_dump ${{ secrets.DATABASE_URL }} > backup.sql

      - name: Upload to R2
        run: |
          aws s3 cp backup.sql s3://my-blog-backups/$(date +%Y%m%d).sql \
            --endpoint-url ${{ secrets.CLOUDFLARE_R2_ENDPOINT }}
```

---

## 대안 서비스 비교표

| 서비스 카테고리 | 옵션 1 | 옵션 2 | 옵션 3 | 비용 | 특징 |
|----------------|--------|--------|--------|------|------|
| **Frontend** | Vercel | Netlify | Cloudflare Pages | $0 | SSG, ISR, Edge |
| **Backend** | Railway | Fly.io | Render | $0-2 | Sleep 모드 |
| **Database** | Neon | Supabase | PlanetScale | $0 | Serverless |
| **Storage** | Cloudflare R2 | Supabase Storage | AWS S3 | $0 | Egress 무료 |
| **Auth** | Custom JWT | Supabase Auth | Clerk | $0 | 통합 인증 |

---

## 결론

**개인 블로그/포트폴리오 추천 구성**:
- **Frontend**: Vercel (무료)
- **Backend**: Railway (무료 $5 크레딧)
- **Database**: Neon (무료)
- **Storage**: Cloudflare R2 (무료)
- **Monitoring**: UptimeRobot (무료)

**예상 비용**: **$0/월**

**확장 경로**:
- 트래픽 증가 시: Vercel Pro ($20/월) 또는 Fly.io ($2/월)로 전환
- DB 용량 부족 시: Neon Pro ($19/월)
- 상업적 사용 시: Vercel Pro 필수

**핵심 전략**:
1. ✅ Static First - ISR 적극 활용
2. ✅ Edge Computing - CDN 최대한 활용
3. ✅ Serverless - 사용한 만큼만 비용
4. ✅ Managed Services - 운영 부담 최소화
5. ✅ 무료 티어 조합 - 월 $0 달성

이 아키텍처로 **99.9% 업타임**, **글로벌 CDN**, **초고속 로딩**을 **월 $0**에 운영 가능합니다! 🚀



---
